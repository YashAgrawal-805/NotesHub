<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Select Semester</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #000; /* Black background */
            color: #fff; /* White text */
            margin: 0;
            padding: 0;
        }

        h1 {
            text-align: center;
            color: #fff; /* White heading */
            margin-top: 30px;
            font-size: 2.5rem;
        }

        /* Semester Container */
        .sem-container {
            display: flex;
            justify-content: center;
            flex-wrap: wrap;
            margin-top: 50px;
        }

        /* Semester Box */
        .sem-box {
            background-color: #222; /* Dark background for boxes */
            color: #f58220; /* Orange text for semesters */
            padding: 0; /* Remove padding */
            border: 1px solid #f58220; /* Orange border */
            border-radius: 10px;
            width: 150px;
            margin: 10px;
            cursor: pointer;
            transition: transform 0.3s, background-color 0.3s, box-shadow 0.3s;
            position: relative; /* Ensure button stays inside the box */
        }

        /* Hover Effect */
        .sem-box:hover {
            transform: scale(1.1);
            background-color: #f58220; /* Orange background on hover */
            color: #000; /* Black text on hover */
            box-shadow: 0 0 10px rgba(255, 255, 255, 0.5);
        }

        /* Button Styling */
        .sem-box button {
            background-color: transparent;
            border: none;
            font-size: 1.2rem;
            color: inherit; /* Inherit color from .sem-box */
            cursor: pointer;
            width: 100%;
            height: 100%; /* Make button cover the entire div */
            display: flex;
            align-items: center;
            justify-content: center;
        }

        /* Remove Button Outline on Focus */
        .sem-box button:focus {
            outline: none;
        }

        @media (max-width: 768px) {
            .sem-box {
                width: 120px; /* Smaller boxes on smaller screens */
            }
        }
        footer{
            margin-top: 20%;
        }
    </style>
</head>
<body>
    <header>
        <%- include('./partials/header2.ejs') %>
    </header>

    <!-- Page Title -->
    <h1>Select Semester</h1>

    <!-- Semester Selection Container -->
    <div class="sem-container">
        <% sems.forEach(function(sem) { %>
            <div class="sem-box">
                <form action="notes/<%= sem %>" method="POST" style="display:inline;">
                    <input type="hidden" name="sem" value="<%= sem %>">
                    <button type="submit"><%= sem %></button>
                </form>
            </div>
        <% }) %>
    </div>
    <footer>
        <%- include('./partials/footer.ejs') %>
    </footer>
</body>
</html>
